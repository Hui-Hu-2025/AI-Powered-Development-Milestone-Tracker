# 前端说明

## 安装依赖

```bash
npm install
```

## 运行开发服务器

```bash
npm start
```

应用将在 http://localhost:3000 打开

## 构建生产版本

```bash
npm run build
```

## 注意事项

- 确保后端API在 http://localhost:8000 运行
- 如果后端运行在不同端口，请修改 `src/App.js` 中的 `API_BASE_URL`
